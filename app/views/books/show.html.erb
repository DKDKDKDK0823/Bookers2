<% if flash[:notice] %>
	<%= flash[:notice] %>
	<% end %>
<div class="header">
	<div class="header1">
		<table>
			<thead>
				<th>Title</th>
				<th>Body</th>
			</thead>
			<tbody>
				<td><%= @book.title %></td>
				<td><%= @book.body %></td>
			</tbody>
		</table>
	        <br>
            <%= link_to "Edit", edit_book_path(@book.id) %>|
            <%= link_to "Back", books_path %>
    </div>
</div>

